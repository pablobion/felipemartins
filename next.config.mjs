/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/felipemartins',
  assetPrefix: '/felipemartins',
};

export default nextConfig; 